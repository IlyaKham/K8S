apiVersion: v1
kind: Service
metadata:
  name: netology-svc
spec:
  type: ClusterIP
  selector:
    app: netology-web
  ports:
  - protocol: TCP
    port: 80          # порт самого Service
    targetPort: 8080  # порт, на котором слушает контейнер